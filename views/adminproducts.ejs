<section class="product">
    <% if(prod != '') { %>
      <% prod.forEach(function(prod, index){ %>
      <div class="content" >
        <div class="box">
          <div class="img1">
            <img src="img/<%= prod.image %>" alt="Hello">
          </div>
          <p><%= prod.name %></p>
          <small>Product category: 
          <div class="prodcat"><%= prod.category %></small></div>
        </div>
    </div>
    <% }) %>
        <% } %>
  </section>